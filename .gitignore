# --- Environments ---
# Ignore virtual environment folders to avoid committing hundreds of library files.
/churn-env/
/venv/
/env/
.env

# --- Python Cache & Compiled Files ---
# Ignore Python's internal cache and compiled files.
__pycache__/
*.pyc
*.pyo
*.pyd

# --- Jupyter Notebook ---
# Ignore the checkpoint files Jupyter creates automatically.
.ipynb_checkpoints/

# --- Data Files ---
# It's good practice to not commit raw data, especially if it's large.
# Keep the raw data folder, but don't track its contents.
/data/raw/*
/data/processed/*
# Use ! to make an exception if you have a small placeholder file you DO want to track.
# Example: !/data/raw/.gitkeep

# --- Models ---
# Trained models are binary files and can be large. They should be generated by code, not stored in Git.
/models/*
!/models/.gitkeep

# --- IDE / Editor specific files ---
# Ignore configuration files from popular editors like VSCode and PyCharm.
.vscode/
.idea/

# --- Operating System specific files ---
# Ignore system files generated by macOS and Windows.
.DS_Store
Thumbs.db